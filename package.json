{
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "setup": "cd backend && prisma generate && prisma db push",
    "dev": "yarn workspaces foreach -A -p -i run dev",
    "build": "yarn workspaces foreach -A -p run build"
  }
}
