{
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "setup": "cd backend && prisma generate && prisma db push",
    "dev": "cd backend && yarn dev & cd frontend && yarn dev"
  }
}
