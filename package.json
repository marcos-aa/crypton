{
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "setup": "yarn workspace backend run setup",
    "dev": "yarn workspaces foreach -A -p -i run dev",
    "build": "yarn workspaces foreach -A -p run build"
  },
  "packageManager": "yarn@4.1.1",
  "devDependencies": {
    "typescript": "^5.4.3"
  }
}
